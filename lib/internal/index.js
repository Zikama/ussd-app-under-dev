(function($){const map = new WeakMap();const internal = function (object){if(!map.has(object))map.set(object, {});return map.get(object);};if (typeof define === 'function' && define.amd) {define(function () {return internal})} else if (typeof module === 'object' && module.exports) {module.exports = internal} else {$.internal = internal}})(this);//this helps to make our class properties more restrictively/private, so you can only access them with code method!